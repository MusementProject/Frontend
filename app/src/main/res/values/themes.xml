<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusementFrontend" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/dark_blue</item>
        <item name="android:windowAnimationStyle">@anim/no_animation</item>

    </style>

    <style name="musement" parent="Base.Theme.MusementFrontend" />
</resources>