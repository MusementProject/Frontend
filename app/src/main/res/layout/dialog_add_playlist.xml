<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="Insert a link to your playlist in Spotify and give it a name"
        android:textColor="@color/blue"
        android:textColorHighlight="@color/light_gray"
        android:textColorHint="@color/blue"
        android:textColorLink="@color/white"
        android:textSize="18sp" />

    <EditText
        android:id="@+id/playlist_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Name your playlist"
        android:inputType="textUri"
        android:textColorHint="@color/light_gray"
        android:textColorLink="@color/blue"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/spotify_link_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="https://open.spotify.com/playlist/..."
        android:inputType="textUri"
        android:textColorHint="@color/light_gray"
        android:textColorLink="@color/blue"
        android:textSize="16sp" />

</LinearLayout>